@model EmployeeManagement.Models.Payroll


@{
    try
    {
        if (TempData["msg7"] != null)
        {
            <script>
                swal("Tony's Employee", "You are not allowed to add record yet!", "info");
            </script>
        }
       

    }
    catch
    {

    }
}


<style>
    .label1 {
        font-size: 36px;
        color: chocolate;
        font-family: Arial;
        font-weight: bold;
        display: block;
    }

    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        border: 1px solid black;
        overflow: hidden;
        background: white;
        border-radius: 6px;
        box-shadow: 0 0 20px rgba(0,0,0,0.1);
        opacity: 1;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        font-weight: bold;
        padding: 13px;
        border-bottom: 1px solid #dddddd;
    }


    body {
        height: 100%;
    }

    body {
        margin: 0;
        background: linear-gradient(45deg, #49a09d, #5f2c82);
        font-family: sans-serif;
        font-weight: 100;
    }

    .form-horizontal {
        border-radius: 6px;
        box-shadow: 0 0 20px rgba(0,0,0,0.1);
        opacity: 1;
        background: transparent;
        padding: 13px;
        font-weight: bold;
    }
</style>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.Label("Payroll", new { @class = "label1" })
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })


        <div class="form-group">
            @Html.LabelFor(model => model.Daily_Salary, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Daily_Salary, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Daily_Salary, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
